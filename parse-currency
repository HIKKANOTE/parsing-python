from urllib.request import urlopen
from xml.etree import ElementTree as etree

with urlopen("https://www.cbr.ru/scripts/XML_daily.asp", timeout=10) as r:
    
    print(etree.parse(r).findtext('.//Valute[@ID="R01200"]/Value'))
